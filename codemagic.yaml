workflows:
  android:
    name: Build APK for Android
    scripts:
      - name: Install dependencies
        script: |
          npm install
      - name: Build APK
        script: |
          eas build --platform android
