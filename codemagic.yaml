workflows:
  android:
    name: Build APK for Android
    description: Build APK for Android using Codemagic
    environment:
      vars:
        EXPO_TOKEN: $EXPO_TOKEN
    steps:
      - name: Install dependencies
        run: |
          npm install
      - name: Build APK
        run: |
          eas build --platform android
